const bcrypt = require('bcryptjs');

class User {
}

// TODO: adaptar essa função
// function generatePassword(raw_password) {
//   const newHash = await bcrypt.hash(raw_password, 10);
//   this.password = newHash;
// }

module.exports = User;
